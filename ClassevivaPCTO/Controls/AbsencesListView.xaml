<UserControl
    x:Class="ClassevivaPCTO.Controls.AbsencesListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClassevivaPCTO.Controls"
    xmlns:adapters="using:ClassevivaPCTO.Adapters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:ClassevivaPCTO.Converters"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>

        <converters:DateTimeToHourConverter x:Key="DateTimeToHourConverter" />


        <DataTemplate x:Key="AbsenceEventListViewDataTemplateCompact"
                  
                  x:DataType="adapters:AbsenceEventAdapter">
            <RelativePanel>


                <TextBlock x:Name="titoloEvento" TextWrapping="Wrap"
                                           MaxWidth="500"
                                           Text="{x:Bind CurrentObject.evtDate}"
                                           Style="{ThemeResource BaseTextBlockStyle}"
                                           Margin="6,6,0,0" />


                <TextBlock RelativePanel.RightOf="titoloEvento" x:Name="justifiedText" TextWrapping="Wrap"
                                           MaxWidth="500"
                                           Text="{x:Bind JustifiedText}"
                                           Style="{ThemeResource BaseTextBlockStyle}"
                                           Margin="6,6,0,0" />




            </RelativePanel>
        </DataTemplate>

    </UserControl.Resources>

    
    
        <ListView x:Name="listView"  ItemTemplate="{StaticResource AbsenceEventListViewDataTemplateCompact}" />
</UserControl>
