<Page
    x:Class="ClassevivaPCTO.Views.Bacheca"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClassevivaPCTO"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:ClassevivaPCTO.Controls"
    mc:Ignorable="d"
    xmlns:microconverters="using:Microsoft.Toolkit.Uwp.UI.Converters"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:toolkit="using:Microsoft.Toolkit.Uwp.UI"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    muxc:BackdropMaterial.ApplyToRootOrPageBackground="True">


    <Page.Resources>
        <ResourceDictionary>
            <microconverters:BoolToVisibilityConverter  x:Key="BoolToVisibilityConverter" />
        </ResourceDictionary>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>


        <RelativePanel Grid.Row="0">

            <TextBlock x:Name="TextTitolo"  Grid.Row="0" Text="Bacheca" FontSize="42" VerticalAlignment="Top"
                   Margin="32,16,0,0" />

            <AppBarButton  RelativePanel.RightOf="TextTitolo" x:Name="AggiornaCommand" VerticalAlignment="Top"  Margin="32,16,0,0" Icon="Refresh" Label="Aggiorna" Click="AggiornaCommand_Click"/>
        </RelativePanel>


        <RelativePanel Grid.Row="2" 
                       Margin="32,32,32,20"
                       VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                       x:Name="CardPanel"
                        toolkit:FrameworkElementExtensions.EnableActualSizeBinding="true"
                CornerRadius="8"
                Padding="12"
                BorderThickness="1"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">



            <muxc:ProgressRing IsActive="True" x:Name="ProgressRingBacheca" 
                           
                               Margin="12,8,0,0"
                                       Visibility="{x:Bind BachecaViewModel.IsLoadingBacheca, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}"/>



            <AutoSuggestBox QueryIcon="Find" 
                            x:Name="SearchBoxBacheca"
                            PlaceholderText="Cerca" RelativePanel.AlignHorizontalCenterWithPanel="True"
                            HorizontalAlignment="Center"
                            Width="260" Height="32" />

            
            <CheckBox x:Name="CheckboxAttive" 
                      Content="Mostra solo comunicazioni attive"
                      Margin="0,0,16,0"
                      RelativePanel.AlignRightWithPanel="True" 
                      RelativePanel.AlignHorizontalCenterWithPanel="True"
                      
                      IsChecked="{x:Bind BachecaViewModel.MostraSoloComAttive, Mode=TwoWay}"/>

            <Grid RelativePanel.Below="SearchBoxBacheca"                           RelativePanel.AlignRightWithPanel="True"
                          RelativePanel.AlignLeftWithPanel="True">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <controls:NoticesListView 
                    
                    Width="{Binding ElementName=CardPanel, Path=(toolkit:FrameworkElementExtensions.ActualWidth)}" 
                    x:Name="NoticesListView"></controls:NoticesListView>

            </Grid>






        </RelativePanel>

    </Grid>
</Page>
