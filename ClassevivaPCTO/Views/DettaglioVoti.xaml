<Page
    x:Class="ClassevivaPCTO.Views.DettaglioVoti"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClassevivaPCTO"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:controls2="using:ClassevivaPCTO.Controls"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    xmlns:animatedvisuals="using:Microsoft.UI.Xaml.Controls.AnimatedVisuals"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    muxc:BackdropMaterial.ApplyToRootOrPageBackground="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <RelativePanel Grid.Row="0" Margin="0,16,0,0">

            <TextBlock x:Name="MainTitleTextBox" x:Uid="TitleValutazioni"
                       Style= "{StaticResource TitleLargeTextBlockStyle}"
                       VerticalAlignment="Top"
                       Margin="32,0,0,0" />

            <muxc:ProgressRing IsActive="False" x:Name="ProgressRingVoti"
                                       RelativePanel.AlignVerticalCenterWith="MainTitleTextBox"
                                       RelativePanel.RightOf="MainTitleTextBox"
                                       Margin="16,0,0,0" Width="28" Height="28"
                                       />

            <Button x:Name="AggiornaCommand"
                    HorizontalAlignment="Right"
                    Width="48"
                    Height="32"
                    Margin="26,0,20,0"  RelativePanel.AlignRightWithPanel="True" RelativePanel.AlignVerticalCenterWith="MainTitleTextBox" ToolTipService.ToolTip="Aggiorna">
                <FontIcon Glyph="&#xE72C;" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"/>
            </Button>


        </RelativePanel>

        <RelativePanel Grid.Row="2"
                       Margin="18"
                       VerticalAlignment="Stretch" HorizontalAlignment="Stretch"
                       CornerRadius="8"
                       Padding="12"
                       BorderThickness="1"
                       BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                       Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">

            <ComboBox x:Name="ComboPeriodi" Header="Periodo didattico" PlaceholderText="Seleziona un periodo"
                      Width="200" SelectionChanged="ComboPeriodi_SelectionChanged">

            </ComboBox>

            <ComboBox RelativePanel.RightOf="ComboPeriodi" x:Name="ComboMaterie" Header="Materia"
                      x:Uid="ComboMaterie" HorizontalAlignment="Left" Margin="25,0,0,0"
                      SelectionChanged="ComboMaterie_SelectionChanged">

            </ComboBox>

            <controls2:GradesListView x:Name="ListViewVoti"

                                      Margin="16,24,0,0"
                                      RelativePanel.Below="ComboPeriodi"
                                      HorizontalAlignment="Stretch" VerticalAlignment="Stretch"

                                      ScrollViewer.VerticalScrollMode="Auto"
                                      ScrollViewer.VerticalScrollBarVisibility="Auto">

            </controls2:GradesListView>

        </RelativePanel>

    </Grid>
</Page>